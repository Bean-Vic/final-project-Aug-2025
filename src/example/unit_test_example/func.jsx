import React from 'react';

export const sum = (a, b) => a + b;


export const C = ({ name }) => {
    return (
        <div>
            <div>test</div>
            <div>Hello, {name}</div>
        </div>
    )
}